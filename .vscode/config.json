{
   "restructuredtext.builtDocumentationPath" : "${workspaceRoot}/project/build/html",
   "restructuredtext.confPath"               : "${workspaceFolder}/project",
   "restructuredtext.updateOnTextChanged"    : "true",
   "restructuredtext.updateDelay"            : 300
}